# CapiFit - Exemplo de variáveis de ambiente do backend (Express)
# Copie para `backend/.env` e configure antes de iniciar em produção.

# Configurações básicas do servidor
PORT=3001
FRONTEND_URL=https://capifit.app.br
FRONTEND_URLS=https://capifit.app.br,http://localhost:5173
JWT_SECRET=altere_esta_chave_super_segura

# Controle de conexão com o banco de dados
# Defina como `true` apenas se quiser iniciar o servidor sem banco (modo mock)
SKIP_DB_CONNECTION=false

# Configuração do banco de dados relacional
VITE_DB_TYPE=mysql
VITE_DB_HOST=localhost
VITE_DB_PORT=3306
VITE_DB_NAME=capifit_db
VITE_DB_USER=capifit_user
VITE_DB_PASSWORD=DefinaUmaSenhaForteAqui
